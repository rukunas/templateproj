============
Installation
============

At the command line::

    $ easy_install templateproj

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv templateproj
    $ pip install templateproj
